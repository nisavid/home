#!/usr/bin/env bash

# Upgrade Vim

ok=1
pushd ~/.spf13-vim-3 > /dev/null \
  && { git pull || unset ok
       popd > /dev/null
       [[ "$ok" ]] || false
     } \
  && vim +BundleInstall! \
  && vim +BundleClean! +NeoBundleUpdate +qa \
  && vim '+PromptlineSnapshot! ~/.promptline.sh airline' +q
