#!/usr/bin/env bash

# Upgrade Vim

ok=1
pushd ~/.vim > /dev/null \
 && { git pull || unset ok
      popd > /dev/null
      [[ -n "$ok" ]] || false
    } \
 && vim +BundleInstall! \
 && vim +BundleClean! +NeoBundleUpdate +qa \
 && vim '+PromptlineSnapshot! ~/.promptline.sh airline' +q
