# shellcheck shell=bash
# Zsh configuration | terminal setup | OS X

[[ $- == *i* ]] || return
# Interactive shell -----------------------------------------------------------

autoload run-help
HELPDIR=/usr/local/share/zsh/help
